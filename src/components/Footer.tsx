export default function Footer() {
  return (
    <footer className="border-t border-border/70 px-4 py-6 text-sm text-muted-foreground">
      <div className="page-shell flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <p className="m-0">Demo inbox for property-management triage, scheduling, and escalation.</p>
        <p className="m-0">Convex + TanStack Start + shadcn + OpenRouter-ready workflow.</p>
      </div>
    </footer>
  );
}
